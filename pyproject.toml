[project]
authors = [
    {name = "Arch4ngel21", email = "adammichal657@gmail.com"},
]
license = {text = "MIT"}
requires-python = "<4.0,>=3.10"
dependencies = [
    "numpy<3.0.0,>=2.2.6",
    "pandas<3.0.0,>=2.3.0",
]
name = "gower-metric"
version = "0.1.0"
description = ""
readme = "README.md"

[tool.setuptools]
packages = ["gower_metric"]

[dependency-groups]
dev = [
    "mypy",
    "pytest>=8.4.1",
]

tests = [
    "pytest",
    "pytest-asyncio<2.0.0,>=1.0.0",
    "setuptools",
    "scipy>=1.15.3",
    "scikit-learn>=1.7.1",
    "mypy",
]

r_env_tests = [
    "pytest",
    "pytest-asyncio<2.0.0,>=1.0.0",
    "setuptools",
    "rpy2",
    "mypy",
]
